import 'package:flutter/material.dart';

/// Cores do aplicativo Bora Né
class AppColors {
  AppColors._();

  /// Cor de fundo principal - #EFEFEF
  static const Color background = Color(0xFFEFEFEF);
  
  /// Cor primária do app
  static const Color primary = Color(0xFFFDB913); // Amarelo da logo
  
  /// Cor secundária do app
  static const Color secondary = Color(0xFF1A1A1A); // Preto da logo
}